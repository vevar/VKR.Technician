<?xml version="1.0" encoding="utf-8"?>
<layout
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:bind="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="viewModel" type="com.nstu.technician.feature.maintenance.MaintenanceViewModel"/>
    </data>
    <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        <LinearLayout
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:setVisibility="@{!viewModel.loader.isLoading()}">
            <include
                    layout="@layout/view_contract"
                    bind:contract="@{viewModel.contract}"/>

            <include
                    layout="@layout/view_detail_maintenance"
                    bind:maintenance="@{viewModel.maintenance}"/>

            <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/list_maintenance_jobs"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:setVisibility="@{viewModel.isListJobsVisible()}"

                    tools:listItem="@layout/view_job">

            </androidx.recyclerview.widget.RecyclerView>
            <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="vertical"
                    app:setVisibility="@{!viewModel.isListJobsVisible()}">
                <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="0dp"
                        android:gravity="center"
                        android:layout_weight="1"
                        tools:ignore="UseCompoundDrawables"
                        android:orientation="vertical">
                    <ImageView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:src="@drawable/ic_center_focus_weak_black"
                            android:contentDescription="@string/lbl_scan_qr_for_start"/>
                    <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/lbl_scan_qr_for_start"
                            android:layout_margin="@dimen/default_margin"
                            android:gravity="center"/>
                </LinearLayout>

                <Button
                        android:id="@+id/btn_scan_qr"
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/fld_height"
                        android:layout_margin="@dimen/default_margin"
                        android:text="@string/btn_scan"
                        android:layout_gravity="end"
                        android:background="@drawable/btn_round_black"
                        android:textColor="@color/white"/>
            </LinearLayout>
        </LinearLayout>

        <ProgressBar
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                app:setVisibility="@{viewModel.loader.isLoading()}"/>
    </FrameLayout>

</layout>