<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools" xmlns:app="http://schemas.android.com/apk/res-auto"
        tools:context=".feature.plan.jobs.map.GMapFragment">
    <data>
        <variable name="viewModel" type="com.nstu.technician.feature.plan.jobs.map.MapViewModel"/>
    </data>
    <FrameLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

        <com.google.android.gms.maps.MapView
                android:id="@+id/mapView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"/>
        <androidx.cardview.widget.CardView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center|top"
                app:cardCornerRadius="25dp"
                android:layout_margin="@dimen/mini_margin"
                app:cardBackgroundColor="@color/black">
            <TextView
                    android:id="@+id/lbl_address"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/white"
                    tools:text="ул.Толстого д.23 оф.5"
                    android:layout_margin="@dimen/default_margin"
                    android:maxLines="1"
                    app:setAddress="@{viewModel.mainFacility}"/>
        </androidx.cardview.widget.CardView>
        <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end|bottom"
                android:orientation="vertical"
                android:layout_marginBottom="100dp"
                android:layout_marginEnd="@dimen/default_margin">

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                    android:id="@+id/btn_place"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:src="@drawable/ic_place_white"
                    android:clickable="true"
                    android:focusable="true"
                    android:layout_margin="@dimen/mini_margin"
                    android:onClick="@{() -> viewModel.goToMainTarget()}"/>
            <com.google.android.material.floatingactionbutton.FloatingActionButton
                    android:id="@+id/btn_my_location"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:src="@drawable/ic_my_location_white"
                    android:clickable="true"
                    android:focusable="true"
                    android:layout_margin="@dimen/mini_margin"
                    android:onClick="@{() -> viewModel.goToDevice()}"/>
        </LinearLayout>
    </FrameLayout>
</layout>